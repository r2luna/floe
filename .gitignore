node_modules
out
dist
*.log
.DS_Store
.env
.env.local

# Copied from @excalidraw/excalidraw on postinstall — 13MB of woff2 that
# belong to the dependency, not to the repo. See scripts/copy-excalidraw-assets.mjs.
src/renderer/public/excalidraw/

# Scratch drawings. The versioned ones live in specs/<branch>/ — see docs/draw.md.
.floe/draw/

# The worktree's standing brief (src/main/premise.ts). It describes ONE branch,
# so it must not travel: committed once, it rode a merge into master and every
# new chat on master opened with another branch's goal.
.floe/premise.md
